spring:
  datasource:  #数据库的配置
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/activiti
    username: root
    password: 1234
  activiti:   #这里如果使用history就用这里的配置
    db-history-used: true
    history-level: full
    database-schema-update: true